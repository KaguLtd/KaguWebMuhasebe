import test from "node:test";

test.skip("remaining legacy acceptance fixtures stay pending until evidence lands", () => {
  // Placeholder moved under legacy-acceptance so missing golden captures remain visible.
});
